package mangogo.appbase.util;

import android.content.Context;
import android.graphics.drawable.Drawable;

//import mangogo.appbase.autolayout.AutoUtils;

public class DrawableUtils {

//    public static Drawable ofDrawable(Context context, int drawableId, int drawableSizeId) {
//        return ofDrawable(context, drawableId, drawableSizeId, drawableSizeId);
//    }

//    public static Drawable ofDrawable(Context context, int drawableId, int drawableWidthId, int drawableHeightId) {
//        Drawable drawable = context.getResources().getDrawable(drawableId);
//        int width, height;
//        if (drawableWidthId == drawableHeightId) {
//            width = height = AutoUtils.getAutoLayoutSize(context, drawableWidthId, 0);
//        } else {
//            width = AutoUtils.getAutoLayoutSize(context, drawableWidthId, 0);
//            height = AutoUtils.getAutoLayoutSize(context, drawableHeightId, 0);
//        }
//        drawable.setBounds(0, 0, width, height);
//        return drawable;
//    }
}
